# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
*.egg-info/
.pytest_cache/

# Virtual Environment
venv/
env/
.env

# Editor/IDE specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Jupyter Notebook files
.ipynb_checkpoints/

# Ignore raw data files and directories, especially downloaded images
# These are typically too large to commit to Git
data/raw/
!data/raw/metadata.json # Keep this if you want to commit metadata.json
!data/raw/.gitkeep       # If you want to keep the raw directory structure but not content

# Ignore processed data, it can be regenerated by the pipeline
data/processed/

# Ignore model artifacts and trained models (can be large)
# These are the directories you previously specified
models/trained_models/
models/artifacts/

# Hugging Face Model Training Output (common Trainer output paths)
# Add these if your models/logs are saved in these specific directories
# This is crucial for ignoring the checkpoints and logs from the fine-tuning process.
output/
runs/
trainer_state.json
training_args.bin

# Saved Model Files (specific files saved by Hugging Face models, if not contained in a main folder like 'models/')
# Include these if they might appear directly in your project root or other unexpected locations
pytorch_model.bin
tf_model.h5
flax_model.msgpack
config.json
tokenizer_config.json
special_tokens_map.json
vocab.txt
merges.txt
tokenizer.json
added_tokens.json

# Logs
*.log
logs/ # General log directory

# Ignore session files generated by telethon (specific to your data ingestion)
*.session
*.session-journal

# Operating System Files
.DS_Store
Thumbs.db